
Let Statements
    let <value> = <expression>

Lists
    [type]{<value, <value>}

Garbage Collection

Finish Buffer Instruction Set

Print Builtin
Characters

For Loop Statements
    for <ident> in <list>
        ...
    end

    for <ident> from <value> to <value>
        ...
    end

List Operators
    union
    append

Functions
    func <ident>(<type> <ident>, ...)
        ...
    end

Structs
    struct <ident>
        <type> <ident>
        ...
    end

Floating Point Numbers

Strings

Fix Character & String Lexing

All unimplemented operators

Cleanup Types (qword, etc);
Newline at file end bug

Rework VM